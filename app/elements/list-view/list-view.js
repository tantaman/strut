(function() {
	// Expect bacon, jquery, etc.?

	/*
	-Needs a data model which is really just an array
	-Needs an item renderer to render the data from our model
		-This will come in via a ctor param or ctor propert
			-data="^model-init;.modelProp"
	*/
})();