define(function() {

	/**
	 * -Determines if we are logged in within the given interval
	 * -Logs us in if not
	 * -Publish JSON content once log-in completes
	 *
	 * -
	 */
	function PublishHandler($btn, editorModel) {

	}

	PublishHandler.prototype = {

	};

	return PublishHandler;
});