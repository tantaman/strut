node_modules
.DS_Store
.uploads
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output

# Generated by `pnpm cf-typegen` (wrangler types). Regenerate on demand; excluded from tsconfig
# because its workerd runtime globals shadow the DOM lib the browser/Node code type-checks against.
worker-configuration.d.ts
.vinxi
__unconfig*
todos.json
*.db
*.db-shm
*.db-wal
*.db-wal2
.dev.vars

# Private commercial overlay (marketing site + Stripe billing). Kept OUT of the open-source tree; it's
# its own private repo cloned/symlinked in here, merged into the build via the #commercial seam.
# See docs/COMMERCIAL_OVERLAY.md.
commercial/
!src/commercial/
!src/commercial/stub.ts
