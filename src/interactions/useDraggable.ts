/*
-onMouseDown: drag start
-onMouseMove: drag
-onMouseUp: dragEnd
-onMouseExit?

Just set some state with current position?
Also fire events about the drag happening
*/

// import type { RefObject } from "react";

// function getTop(ref: RefObject<HTMLElement>) {

// }

// function getLeft(ref: RefObject<HTMLElement>) {

// }

// export default function useDraggable(ref: RefObject<HTMLElement>) {
//   // or just a HOC
//   const top = useState(getTop(ref));
//   const left = useState(getLeft(ref));

//   // somehow add a listener to the react component using this hook...
//   // or return an object they can call to wire up events?
// }
