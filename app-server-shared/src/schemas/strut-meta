CREATE TABLE IF NOT EXISTS "deck_map" (
  "title" TEXT,
  "dbid" BLOB PRIMARY KEY,
  "lastModified" INTEGER,
  "isDirty" INTEGER
) STRICT;

CREATE INDEX IF NOT EXISTS "deck_map_touched" ON "deck_map" ("lastModified");