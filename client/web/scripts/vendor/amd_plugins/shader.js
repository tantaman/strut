define(function() {
	return {
		//node.attachEvent("onreadystatechange", callback);
	    load: function (name, req, load, config) {
	    	var shaderTag = document.createElement("script");
	    	shaderTag.type = "x-shader/x-fragment";
	    }
	}
});